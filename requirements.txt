# Web deployment (Vercel) — the only dependency the live dashboard needs.
# The command-line tool (main.py) itself uses just the Python standard library.
flask>=3.0

# The optional ML module has its own dependencies: see ml/requirements.txt
